import 'package:flutter_test/flutter_test.dart';
import 'package:loading_button_plus/loading_button.dart';

void main() {
  test('adds one to input values', () {
    final loadingButton = LoadingButtonPlus(text: "Text", onTap: (){});
  });
}
