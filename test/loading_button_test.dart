import 'package:flutter_test/flutter_test.dart';

import 'package:loading_button/loading_button.dart';

void main() {
  test('adds one to input values', () {
    final loadingButton = LoadingButton(text: "Text", onTap: (){});
  });
}
